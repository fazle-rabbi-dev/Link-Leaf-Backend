info:
  name: 🔐 Reset password
  type: http
  seq: 7

http:
  method: OPTIONS
  url: "{{base_url}}/reset-password"
  body:
    type: form-urlencoded
    data:
      - name: token
        value: japener791@94an.com
      - name: newPassword
        value: "000   "
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
