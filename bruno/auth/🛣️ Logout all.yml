info:
  name: 🛣️ Logout all
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{base_url}}/logout-all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
