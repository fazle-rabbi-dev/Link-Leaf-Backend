info:
  name: Get public profile
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/antonio"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
