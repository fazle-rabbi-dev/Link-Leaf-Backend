info:
  name: SEO
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{base_url}}/seo"
  body:
    type: form-urlencoded
    data:
      - name: f
        value: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
