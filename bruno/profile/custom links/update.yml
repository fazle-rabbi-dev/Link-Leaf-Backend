info:
  name: update
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{custom_link_base_url}}/6a9665fc702317edc2ed4461"
  body:
    type: json
    data: |-
      {
        "title": "abc",
        "shouldRemoveIcon": true
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
