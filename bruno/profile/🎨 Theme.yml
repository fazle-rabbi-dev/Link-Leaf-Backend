info:
  name: 🎨 Theme
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{base_url}}/theme"
  body:
    type: json
    data: |2
        {
        "type": "preset",
        "preset": "leaf"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
