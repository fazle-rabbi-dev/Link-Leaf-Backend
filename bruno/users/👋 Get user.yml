info:
  name: 👋 Get user
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/me"
  body:
    type: form-urlencoded

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
